# Extracted Balatro game source — copyrighted by the developer, kept as a
# LOCAL reference only. Never commit game code.
game_src/

# Packaged mod build output
*.zip
# Generated Loadstone release manifest (built with the zips, uploaded alongside)
/loadstone-release.json

# Local version stamp (deploy.ps1 / post-merge hook / build_release.ps1)
/src/version

# Bundled docs, copied from the repo root by deploy.ps1 / build_release.ps1
/src/docs/
BlindfoldInstaller.exe

# Rust build output
installer/target/

# Runtime log
*.log

# OS / editor cruft
.DS_Store
Thumbs.db
.vs/
.idea/
.vscode/
